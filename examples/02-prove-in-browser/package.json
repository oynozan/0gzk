{
  "name": "0gzk-example-prove-in-browser",
  "version": "0.0.0",
  "private": true,
  "description": "Browser-side ZK proving: fetch a 0gzk bundle directly from 0G Storage, gunzip+untar in the browser, generate a Groth16 proof with snarkjs, verify locally. No backend.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@0gzk/sdk": "^0.2.0",
    "ethers": "^6.13.0",
    "fflate": "^0.8.2",
    "snarkjs": "^0.7.5"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^5.4.10"
  },
  "engines": {
    "node": ">=20"
  }
}
